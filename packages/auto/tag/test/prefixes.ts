import type { TPrefixes } from '@nextnextools/auto-core'

export const prefixes: TPrefixes = {
  major: '💥',
  minor: '🌱',
  patch: '🐞',
  publish: '📦',
  dependencies: '♻️',
  initial: '🐣️',
}
